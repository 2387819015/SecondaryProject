<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.secondaryproject.mapper.OrderMapper">
    <select id="selectOrderByUserId" parameterType="Integer" resultType="com.example.secondaryproject.pojo.order">
        SELECT * from order_table where user_id = #{userId}
    </select>
</mapper>