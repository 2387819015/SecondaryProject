<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.secondaryproject.mapper.CartMapper">
    <select id="deleteCart" parameterType="Integer" >
        DELETE from cart_table where goods_id = #{goodsId} and goods_num = #{goodsNum} and user_id = #{userId} limit 1
    </select>
</mapper>



